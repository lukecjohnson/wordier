{
  "private": true,
  "scripts": {
    "build": "esbuild src/app.js src/styles.css \"--external:*.woff2\" --bundle --minify --outdir=public",
    "dev": "esbuild src/app.js src/styles.css \"--external:*.woff2\" --bundle --outdir=public --servedir=public"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
